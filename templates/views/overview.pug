extends ../layouts/default

block content
	.container: .jumbotron
		img(src='/images/logo.svg', width='160')
		h1 Übersicht
		p Herzlich willkommen, #{username}!

		ul
			for event, i in events
				li= event.title
